[project]
name = "finance-mcp"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.3",
    "gspread>=6.0",
    "google-auth>=2.0",
    "pydantic>=2.0",
    "python-dotenv>=1.0",
]
